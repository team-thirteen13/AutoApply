---
phase: 03-footer-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/footer.tsx
  - src/app/page.tsx
  - src/test/footer.test.tsx
autonomous: true
requirements:
  - LP-08
must_haves:
  truths:
    - Footer renders 4 link columns (Product, Resources, Company, Legal) with correct link text and hrefs
    - Footer displays dynamic copyright year matching current year
    - Footer has role="contentinfo" landmark for screen readers
    - Bottom section shows copyright text plus Privacy and Terms links
    - Gradient transition fades from last CTA section into dark footer background
    - Columns stack vertically on mobile (grid-cols-2) and display 4-across on md+ (md:grid-cols-4)
  artifacts:
    - src/components/landing/footer.tsx
    - src/test/footer.test.tsx
  key_links:
    - Footer appended after last CTASection in src/app/page.tsx
    - Gradient transition connects Testimonials/CTA background to footer gray-900
---

<objective>
Build the landing page footer component and integrate it into the page. The footer provides navigation links organized in 4 columns, a dynamic copyright year, and a bottom accessibility bar with repeated key links. A gradient transition connects the last content section to the dark footer background.

Purpose: Completes the page structure (LP-08) and provides the visual anchor that signals page completion to visitors.
Output: Footer component, gradient integration in page.tsx, and comprehensive test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-footer-polish/03-CONTEXT.md
@.planning/phases/03-footer-polish/03-RESEARCH.md
@src/app/page.tsx
@src/components/landing/landing-layout.tsx
@src/app/globals.css
@src/test/feature-showcase.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Footer component with 4-column layout and gradient transition</name>
  <files>src/components/landing/footer.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/landing/landing-layout.tsx (current layout wrapper pattern)
    - src/app/page.tsx (current page structure — Footer goes after last CTASection)
    - src/app/globals.css (color tokens — gray-900 for footer bg, existing gradient patterns)
    - src/components/landing/cta-section.tsx (gradient pattern to match for footer transition)
    - src/components/landing/navbar.tsx (max-w-6xl pattern for contained layout)
  </read_first>
  <behavior>
    - Footer renders 4 link columns: Product (Features /pricing, Pricing /pricing), Resources (Blog /blog, Help Center /help), Company (About /about, Contact /contact), Legal (Privacy Policy /privacy, Terms of Service /terms)
    - Each column has a heading (uppercase, white, semibold, text-sm) and an unordered list of links
    - Footer has role="contentinfo" attribute on the outermost element
    - Bottom section displays copyright with dynamic year: text matches pattern "© {YYYY} AutoApply. All rights reserved." where YYYY is the current 4-digit year
    - Bottom section also displays Privacy (/privacy) and Terms (/terms) repeated links
    - A border-t (gray-700) separates the columns section from the bottom section
    - A gradient div sits above the footer element with classes: bg-gradient-to-b from-transparent to-gray-900, h-24
    - Footer element has bg-gray-900 text-gray-300 py-12
    - Content container has max-w-6xl mx-auto px-4 sm:px-6 lg:px-8
    - Grid uses grid-cols-2 md:grid-cols-4 gap-8
    - Links have text-sm hover:text-white transition-colors
  </behavior>
  <action>
Create src/components/landing/footer.tsx as a SERVER component (no "use client" directive). Define a footerLinks object with four keys (product, resources, company, legal), each containing an array of { label, href } objects matching the exact link text and paths from D-01. Export a named function Footer that renders: (1) a gradient div before the footer with bg-gradient-to-b from-transparent to-gray-900 h-24, (2) the footer element with role="contentinfo", bg-gray-900, text-gray-300, py-12, (3) inside: a max-w-6xl container with px-4 sm:px-6 lg:px-8, (4) a grid with grid-cols-2 md:grid-cols-4 gap-8 mapping over footerLinks entries — each column renders an h3 with category name (uppercase, white, font-semibold, text-sm, tracking-wider, mb-4) and a ul with space-y-2 containing li elements with link text and hover styles, (5) a bottom section with border-t border-gray-700 mt-8 pt-8 flex flex-col sm:flex-row justify-between items-center text-sm containing the copyright paragraph with dynamic year via new Date().getFullYear() and a div with Privacy and Terms links.

Modify src/app/page.tsx: import Footer from "@/components/landing/footer" and add <Footer /> after the last CTASection, outside and after the LandingLayout closing tag (so the gradient div sits between the last CTA and the footer). Since LandingLayout wraps all current sections, place Footer after the LandingLayout closing tag so it sits at the page root level.

Per D-06: bg-gray-900 with text-gray-300 for the dark footer.
Per D-04: max-w-6xl container matches hero and other sections.
Per D-05: Bottom section with copyright + repeated Privacy/Terms links.
Per D-07: Gradient div (from-transparent to-gray-900) provides the visual transition.
Per D-08: grid-cols-2 md:grid-cols-4 for mobile stacking.
Per D-03: Dynamic copyright year via new Date().getFullYear() in server component.
Per D-02: No social media icons — footer is link-only.
  </action>
  <verify>
    <automated>npm run test -- --testPathPattern footer && npm run lint && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/landing/footer.tsx exists and does NOT contain "use client" (server component)
    - grep -c 'role="contentinfo"' src/components/landing/footer.tsx returns 1
    - grep -c 'grid-cols-2 md:grid-cols-4' src/components/landing/footer.tsx returns 1
    - grep -c 'new Date().getFullYear()' src/components/landing/footer.tsx returns 1
    - grep -c 'bg-gray-900' src/components/landing/footer.tsx returns at least 1
    - grep -c 'from-transparent to-gray-900' src/components/landing/footer.tsx returns 1
    - src/app/page.tsx contains import of Footer and renders <Footer /> after the last CTASection
    - npm run test -- --testPathPattern footer passes with 0 failures
    - npm run lint passes with 0 errors
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <done>
    Footer component renders 4 link columns with correct text and hrefs, dynamic copyright year, role="contentinfo", gradient transition, and is integrated at the bottom of the page. All automated checks pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write comprehensive footer tests</name>
  <files>src/test/footer.test.tsx</files>
  <read_first>
    - src/components/landing/footer.tsx (the component being tested — must read after Task 1 creates it)
    - src/test/feature-showcase.test.tsx (test file pattern: @vitest-environment jsdom, render from @testing-library/react, cleanup)
    - src/test/testimonials.test.tsx (additional test pattern reference)
    </read_first>
  <behavior>
    - Test: Footer renders all 4 column headings (Product, Resources, Company, Legal)
    - Test: Footer renders all 8 link items with correct labels (Features, Pricing, Blog, Help Center, About, Contact, Privacy Policy, Terms of Service)
    - Test: All links have correct href attributes
    - Test: Footer has role="contentinfo" attribute
    - Test: Copyright text contains current year (4-digit number matching new Date().getFullYear())
    - Test: Copyright text contains "AutoApply"
    - Test: Bottom section renders Privacy and Terms repeated links
    - Test: Footer element has bg-gray-900 class (dark background per D-06)
    - Test: Gradient div with from-transparent to-gray-900 exists (D-07)
    - Test: Grid uses responsive classes (grid-cols-2 for mobile, md:grid-cols-4 for desktop per D-08)
  </behavior>
  <action>
Create src/test/footer.test.tsx with @vitest-environment jsdom directive at top. Import { describe, it, expect } from "vitest" and { render, screen } from "@testing-library/react". Import Footer from "@/components/landing/footer".

Write a describe("Footer") block with it() tests for each behavior above. Use screen.getByRole("contentinfo") to locate the footer element. Use screen.getAllByRole("link") or screen.getByText() for link assertions. Use toHaveAttribute for href checks. Use toContainText or textContent checks for copyright year. Use toHaveClass for gradient and background assertions.

Follow the existing test pattern: cleanup after each test, @vitest-environment jsdom at file top. Use afterEach with cleanup from @testing-library/react.
  </action>
  <verify>
    <automated>npm run test -- --testPathPattern footer</automated>
  </verify>
  <acceptance_criteria>
    - File src/test/footer.test.tsx exists with @vitest-environment jsdom directive on line 1
    - Test suite contains at least 8 test cases (it blocks)
    - All tests pass (npm run test -- --testPathPattern footer exits 0)
    - Tests verify role="contentinfo", 4 column headings, 8 link items, dynamic year, gradient class, responsive grid classes
  </acceptance_criteria>
  <done>
    All footer tests pass, covering 4-column layout, link text and hrefs, dynamic copyright year, ARIA landmark, gradient transition, dark background, and responsive grid classes.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
- src/components/landing/footer.tsx — Footer server component (exported function Footer)
- src/test/footer.test.tsx — Footer test suite (describe("Footer") with 8+ test cases)
- Modified: src/app/page.tsx — Footer import and <Footer /> rendered after last CTASection
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client rendering | Footer links rendered in browser; href values are static strings, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Footer links | low | accept | All hrefs are relative paths to internal pages; no external URLs, no sensitive data in link text |
| T-03-02 | Tampering | npm install focus-trap-react | high | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages (deferred to Plan 02 if needed) |
</threat_model>

<verification>
- npm run test -- --testPathPattern footer passes
- npm run lint passes
- npm run typecheck passes
- Footer renders 4 columns with correct link text at all viewports
- Gradient transition visually connects last section to dark footer
</verification>

<success_criteria>
- Footer component exists at src/components/landing/footer.tsx with 4-column layout, dynamic copyright, role="contentinfo"
- Footer is rendered at the bottom of the landing page in src/app/page.tsx
- Gradient transition (from-transparent to-gray-900) bridges content to footer
- All footer tests pass (8+ test cases covering structure, links, a11y, styling)
- Lint and typecheck pass with zero errors
</success_criteria>

<output>
Create `.planning/phases/03-footer-polish/03-01-SUMMARY.md` when done
</output>
