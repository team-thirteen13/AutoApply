---
phase: 03-footer-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/footer.tsx
  - src/test/footer.test.tsx
  - src/components/landing/cta-section.tsx
  - src/app/page.tsx
autonomous: true
gap_closure: true
requirements:
  - LP-08

must_haves:
  truths:
    - Footer gradient transition div is removed
    - Per-section gradient colors are applied consistently across all landing sections
    - No single gradient color is used for the whole page
  artifacts:
    - src/components/landing/footer.tsx
    - src/components/landing/cta-section.tsx
  key_links:
    - Footer renders without gradient transition div
    - Each section maintains its own gradient color scheme

---

<objective>
Remove the gradient transition div from the footer and implement per-section gradient colors instead of using the main gradient color for the whole page.

Purpose: The user reported that the gradient transition in the footer is not desired, and wants each section to have its own suitable gradient colors rather than using a single gradient for the entire page.

Output: Updated footer component with gradient transition removed, consistent per-section gradient colors across all landing sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-footer-polish/03-01-SUMMARY.md
@.planning/phases/03-footer-polish/03-UAT.md
@src/components/landing/footer.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove gradient transition div from footer</name>
  <files>src/components/landing/footer.tsx</files>
  <action>
    Remove the gradient transition div from the footer component.

    NOTE: This overrides D-07 ("Gradient fade from content to footer"). The UAT finding (test 1) explicitly requested removal of this gradient transition, which supersedes the original design decision.

    1. In `src/components/landing/footer.tsx`, remove the div element at line 42:
       `<div className="bg-gradient-to-b from-transparent to-gray-900 h-24" />`

    2. This div creates a 24-unit tall gradient transition from transparent to gray-900.
       The user requested this be removed per UAT test 1.

    3. Keep the footer's own background styling (bg-gray-900 text-gray-300 py-12) intact.

    4. The footer should now render directly after the last CTA section without a gradient transition.
  </action>
  <verify>
    <automated>npm run test -- --testPathPattern footer && [ "$(grep -c 'bg-gradient-to-b' src/components/landing/footer.tsx)" = "0" ]</automated>
  </verify>
  <done>
    - Gradient transition div removed from footer.tsx
    - Footer renders without the 24-unit gradient transition
    - All footer tests pass
    - grep for "bg-gradient-to-b" in footer.tsx returns 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement per-section gradient colors</name>
  <files>src/components/landing/cta-section.tsx</files>
  <action>
    Ensure per-section gradient colors are applied consistently across all landing sections:

    1. The existing sections already have per-section gradients:
       - feature-card.tsx: gradient prop (default: from-blue-900/80 to-slate-800/90)
       - workflow-step.tsx: gradient prop (default: from-blue-900/80 to-slate-800/90)
       - testimonial-card.tsx: gradient prop (default: from-blue-900/80 to-slate-800/90)
       - cta-section.tsx: bg-gradient-to-br from-blue-900/80 to-slate-800/90

    2. The CTASection component uses a hardcoded gradient. Update it to accept a gradient prop
       with a default value, similar to how feature-card, workflow-step, and testimonial-card work.

    3. In `src/components/landing/cta-section.tsx`:
       - Add a `gradient` prop with type `string` and default value `"from-blue-900/80 to-slate-800/90"`
       - Use the gradient prop in the className: `bg-gradient-to-br ${gradient}`

    4. Update `src/app/page.tsx` to pass different gradient values to each CTASection instance:
       - First CTASection (line 22-25): Keep default gradient or pass a specific color
       - Second CTASection (line 29-33): Pass a different gradient like "from-purple-900/80 to-indigo-800/80"

    5. This ensures each section has its own suitable gradient color rather than using a single
       gradient for the whole page, as requested in UAT test 1.
  </action>
  <verify>
    <automated>npm run test -- --testPathPattern "cta|feature|testimonial|workflow" && npm run typecheck</automated>
  </verify>
  <done>
    - CTASection accepts gradient prop with default value
    - Each CTASection instance uses its own gradient color
    - All section tests pass
    - TypeScript compilation succeeds
    - Each section has distinct gradient colors (no single gradient for whole page)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No new trust boundaries introduced — UI-only changes to existing components |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | N/A | footer.tsx | low | accept | No security impact — gradient styling change only |
| T-03-02 | N/A | cta-section.tsx | low | accept | No security impact — prop addition with default value |

</threat_model>

<verification>
1. Run `npm run test` — all tests pass
2. Run `npm run lint` — no errors
3. Run `npm run typecheck` — no errors
4. Run `grep -c "bg-gradient-to-b" src/components/landing/footer.tsx` — returns 0 (gradient removed)
5. Run `grep "gradient" src/components/landing/cta-section.tsx` — shows gradient prop usage
6. Visual verification: Footer renders without gradient transition, each section has its own gradient colors
</verification>

<success_criteria>
1. Footer gradient transition div is completely removed
2. CTASection component accepts gradient prop with default value
3. Each CTASection instance in page.tsx uses different gradient colors
4. All automated tests pass
5. No TypeScript errors
6. No lint errors
</success_criteria>

<output>
Create `.planning/phases/03-footer-polish/03-03-SUMMARY.md` when done
</output>
