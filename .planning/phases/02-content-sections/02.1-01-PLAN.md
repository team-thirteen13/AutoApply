---
phase: 02-content-sections
plan: "02.1-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/landing/hero.tsx
  - src/components/landing/feature-showcase.tsx
  - src/components/landing/feature-card.tsx
  - src/components/landing/ai-workflow.tsx
  - src/components/landing/testimonials.tsx
  - src/test/feature-showcase.test.tsx
autonomous: true
requirements: [LP-02, LP-03, LP-04]
gap_closure: true

must_haves:
  truths:
    - "Each section transition uses a diagonal/angled cut via CSS clip-path, not a gradient fade"
    - "Each section has its own matched gradient color token defined in globals.css"
    - "Hero section uses clip-path for its bottom transition instead of an absolute-positioned overlay div"
    - "Feature showcase section uses custom inline SVG components instead of Lucide React icons"
    - "FeatureCard accepts and renders SVG elements passed as children or render prop"
    - "All 6 feature cards display custom SVG icons specific to their feature"
    - "Feature showcase tests assert custom SVG rendering instead of Lucide icon data-testid attributes"
  artifacts:
    - src/app/globals.css (per-section color tokens added)
    - src/components/landing/hero.tsx (clip-path transition)
    - src/components/landing/feature-showcase.tsx (custom SVG icons, clip-path section)
    - src/components/landing/feature-card.tsx (updated icon rendering)
    - src/components/landing/ai-workflow.tsx (clip-path section)
    - src/components/landing/testimonials.tsx (clip-path section)
    - src/test/feature-showcase.test.tsx (updated assertions)
  key_links:
    - "globals.css color tokens consumed by hero, feature-showcase, ai-workflow, testimonials section classes"
    - "FeatureCard icon prop accepts SVG element, feature-showcase passes inline SVGs"
---

<objective>
Close Gap 1 (Section Visual Treatment) and Gap 3 (Feature Icons) from the Phase 2 UAT diagnosis.

Purpose: Replace gradient fade transitions between sections with diagonal/angled cuts using CSS clip-path, define per-section gradient color tokens, and replace all Lucide React icons in the feature showcase with custom inline SVG components.

Output: Updated globals.css with section color tokens, all four content sections using clip-path transitions, feature-showcase using custom SVG icons, and passing tests.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-UAT.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@.planning/phases/02-content-sections/02-02-SUMMARY.md
@.planning/phases/02-content-sections/02-03-SUMMARY.md
@src/app/globals.css
@src/components/landing/hero.tsx
@src/components/landing/feature-showcase.tsx
@src/components/landing/feature-card.tsx
@src/components/landing/ai-workflow.tsx
@src/components/landing/testimonials.tsx
@src/test/feature-showcase.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section clip-path transitions and gradient color tokens</name>
  <files>
    src/app/globals.css,
    src/components/landing/hero.tsx,
    src/components/landing/feature-showcase.tsx,
    src/components/landing/ai-workflow.tsx,
    src/components/landing/testimonials.tsx
  </files>
  <action>
**1. Add per-section gradient color tokens to globals.css (src/app/globals.css):**

Inside the `@theme inline` block, add new color tokens for each section's gradient theme. These replace the current flat gradient approach with distinct per-section color identities:

```
--color-features-start: #f0f9ff;
--color-features-end: #e0f2fe;
--color-ai-start: #f0fdf4;
--color-ai-end: #ecfdf5;
--color-testimonials-start: #faf5ff;
--color-testimonials-end: #f5f3ff;
```

Keep existing hero-start, hero-end, accent tokens unchanged. The section tokens provide distinct light gradient themes per section.

**2. Add reusable clip-path utility classes to globals.css** (after the @theme block):

```
.section-cut-bottom {
  clip-path: polygon(0 0, 100% 0, 100% calc(100% - 40px), 0 100%);
}
.section-cut-top {
  clip-path: polygon(0 40px, 100% 0, 100% 100%, 0 100%);
}
```

These create angled diagonal cuts at the bottom or top of sections. The 40px offset creates a visible diagonal.

**3. Update hero.tsx (src/components/landing/hero.tsx):**

- Remove the absolute-positioned gradient fade div at line 52: `<div className="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-white to-transparent" />`
- Add `section-cut-bottom` class to the section element (alongside existing classes)
- The section already has `overflow-hidden` which is required for clip-path to work

The hero section element becomes: `className="relative min-h-screen overflow-hidden bg-gradient-to-br from-hero-start to-hero-end flex items-center justify-center section-cut-bottom"`

**4. Update feature-showcase.tsx (src/components/landing/feature-showcase.tsx):**

- Replace `bg-gradient-to-b from-white to-slate-50` with a solid background using the section token: `bg-features-start`
- Add `section-cut-bottom` class to the section element
- Remove the `bg-gradient-to-b` classes entirely

The section element becomes: `className="py-24 bg-features-start section-cut-bottom"`

**5. Update ai-workflow.tsx (src/components/landing/ai-workflow.tsx):**

- Replace `bg-gradient-to-b from-slate-50 to-white` with: `bg-ai-start`
- Add `section-cut-bottom` class to the section element

The section element becomes: `className="py-24 bg-ai-start section-cut-bottom"`

**6. Update testimonials.tsx (src/components/landing/testimonials.tsx):**

- Replace `bg-gradient-to-b from-white to-slate-50` with: `bg-testimonials-start`
- Add `section-cut-bottom` class to the section element

The section element becomes: `className="py-24 bg-testimonials-start section-cut-bottom"`
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run typecheck && npm run lint && grep -c "section-cut-bottom" src/components/landing/hero.tsx src/components/landing/feature-showcase.tsx src/components/landing/ai-workflow.tsx src/components/landing/testimonials.tsx | grep -v ":0$" && grep -c "features-start\|ai-start\|testimonials-start" src/app/globals.css</automated>
  </verify>
  <done>
    - globals.css has 4 new section color tokens (features-start, features-end, ai-start, ai-end, testimonials-start, testimonials-end)
    - globals.css has section-cut-bottom and section-cut-top utility classes
    - hero.tsx has section-cut-bottom class and no absolute gradient overlay div
    - feature-showcase.tsx uses bg-features-start and section-cut-bottom
    - ai-workflow.tsx uses bg-ai-start and section-cut-bottom
    - testimonials.tsx uses bg-testimonials-start and section-cut-bottom
    - npm run typecheck passes
    - npm run lint passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Custom SVG icons for feature showcase</name>
  <files>
    src/components/landing/feature-showcase.tsx,
    src/components/landing/feature-card.tsx,
    src/test/feature-showcase.test.tsx
  </files>
  <action>
**1. Create inline SVG icon components in feature-showcase.tsx:**

Remove the Lucide React imports (FileText, User, Briefcase, GraduationCap, FolderOpen, Wrench) from the top of the file. Define 6 inline SVG icon components directly in the file. Each icon should be a functional component that accepts `{ className?: string }` and returns an SVG element. Use `currentColor` for the fill/stroke so the icon inherits text color from its parent.

The 6 icons to create:

- **ResumeCreationIcon**: A document/page icon with a folded corner and horizontal lines representing text. Use a 24x24 viewBox. The document body is a rounded rectangle with a small triangle fold at the top-right corner, and 3 horizontal lines inside.
- **ProfileManagementIcon**: A person silhouette (head circle + shoulders/body arc) inside a rounded rectangle frame, suggesting a profile card.
- **ExperienceTrackingIcon**: A briefcase icon with a handle on top and a rectangular body, suggesting work experience.
- **EducationIcon**: A graduation cap (mortarboard) shape: a diamond/rhombus top with a tassel line hanging from one side, and a horizontal band below.
- **ProjectListingsIcon**: A folder icon with a tab on the left side and a document peeking out slightly from inside.
- **SkillManagementIcon**: A wrench or gear icon. Use a hexagonal gear shape with teeth around the perimeter and a circle in the center.

Each component should be approximately 20-30 lines of SVG code. Use `stroke="currentColor"` and `fill="none"` for line-art style icons, or `fill="currentColor"` for solid icons. The `className` prop should be applied to the outer `<svg>` element.

**2. Update the features array in feature-showcase.tsx:**

Replace the icon property values:
```
{ icon: ResumeCreationIcon, title: "Resume Creation", ... }
{ icon: ProfileManagementIcon, title: "Profile Management", ... }
{ icon: ExperienceTrackingIcon, title: "Experience Tracking", ... }
{ icon: EducationIcon, title: "Education", ... }
{ icon: ProjectListingsIcon, title: "Project Listings", ... }
{ icon: SkillManagementIcon, title: "Skill Management", ... }
```

**3. Verify feature-card.tsx (src/components/landing/feature-card.tsx) icon rendering:**

The current FeatureCardProps interface already accepts `icon: React.ComponentType<{ className?: string }>`, which works with both Lucide components and custom SVG components. The rendering at line 18 uses `<Icon className="h-6 w-6 text-hero-start" />`. This is compatible with the new SVG components as long as they accept a className prop and apply it to the root SVG element. No changes needed to feature-card.tsx unless the SVG components use a different prop signature -- verify the type compatibility.

**4. Update feature-showcase.test.tsx (src/test/feature-showcase.test.tsx):**

Remove the lucide-react mock block (lines 27-34) since feature-showcase no longer imports from lucide-react. Remove the `import { FileText } from "lucide-react"` on line 40.

Update the FeatureCard test: Instead of mocking a Lucide icon, create a simple test SVG component inline:
```
const TestIcon = ({ className }: { className?: string }) => (
  <svg className={className} data-testid="test-icon"><circle /></svg>
);
```

Use this as the icon prop in FeatureCard tests. Update the icon container assertion to check for the custom icon rendering instead of `bg-hero-start/10` if needed.

Update FeatureShowcase tests:
- Remove any assertions that relied on Lucide mock testids
- The "renders all 6 feature cards" test should still pass since it checks text content
- Add a test that verifies the section has `bg-features-start` class instead of `bg-gradient-to-b from-white to-slate-50`
- Add a test that verifies the section has `section-cut-bottom` class
- Remove the test that checks for `bg-gradient-to-b from-white to-slate-50` and replace with the new background assertion
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run test -- --filter=feature-showcase 2>&1 | tail -20</automated>
  </verify>
  <done>
    - feature-showcase.tsx has 6 custom inline SVG icon components (no lucide-react imports)
    - feature-showcase.tsx features array uses custom SVG icons
    - feature-card.tsx renders custom SVG icons correctly (type-compatible)
    - feature-showcase.test.tsx has no lucide-react mocks
    - feature-showcase.test.tsx tests pass for all 6 feature cards
    - feature-showcase.test.tsx asserts bg-features-start and section-cut-bottom on section
    - npm run test passes for feature-showcase
    - npm run typecheck passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS injection via class names | Section color tokens and clip-path classes are static strings, no user input |
| SVG rendering in components | Inline SVGs are hardcoded, not user-generated content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan -- only CSS and SVG changes |
| T-02.1-01-01 | Information Disclosure | globals.css color tokens | low | accept | Tokens are visual styling only, no sensitive data |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm run lint` passes
- `npm run test -- --filter=feature-showcase` passes
- All 4 sections have `section-cut-bottom` class
- globals.css has 6 new section color tokens and 2 clip-path utility classes
- feature-showcase.tsx has no lucide-react imports
- All 6 custom SVG icon components render correctly
</verification>

<success_criteria>
- Diagonal/angled cuts replace gradient fades at section boundaries
- Each section has its own gradient color theme via globals.css tokens
- Hero overlay div is removed in favor of clip-path
- Feature showcase uses 6 custom inline SVG icons
- Tests pass for updated icon and section assertions
</success_criteria>

<output>
Create `.planning/phases/02-content-sections/02.1-01-SUMMARY.md` when done
</output>
