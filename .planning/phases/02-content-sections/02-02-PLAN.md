---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/landing/workflow-step.tsx
  - src/components/landing/ai-workflow.tsx
  - src/components/landing/cta-section.tsx
  - src/app/page.tsx
  - src/test/ai-workflow.test.tsx
autonomous: true
requirements: [LP-03]

must_haves:
  truths:
    - "AI workflow preview section is visible below the feature showcase"
    - "4-step pipeline displays horizontally on desktop with arrow connectors between steps"
    - "Pipeline stacks vertically on mobile with down-arrow connectors"
    - "Each step shows a numbered circle, icon, and short label"
    - "Section headline reads 'Your AI-powered job search' (D-08)"
    - "A sign-up CTA button appears below the pipeline (D-09, first CTA)"
  artifacts:
    - src/components/landing/workflow-step.tsx
    - src/components/landing/ai-workflow.tsx
    - src/components/landing/cta-section.tsx
    - src/app/page.tsx (modified)
    - src/test/ai-workflow.test.tsx
  key_links:
    - "feature-showcase.tsx bottom connects to ai-workflow.tsx top via gradient"
    - "CTASection links to /register via Button component"
    - "page.tsx renders AIWorkflow and CTASection in correct order after FeatureShowcase"
---

<objective>
Create the AI Workflow Preview section (LP-03): a 4-step horizontal pipeline with arrow connectors, plus a reusable CTASection component and the first sign-up CTA.

Purpose: Shows visitors the upcoming AI-powered job application pipeline. Builds excitement for future features while grounding them in the current product. The CTASection is reusable for the second CTA in Plan 3.

Output: WorkflowStep component, AIWorkflow section, CTASection component, page integration, and smoke tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@src/app/page.tsx
@src/components/landing/feature-showcase.tsx
@src/components/ui/button.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WorkflowStep component and CTASection</name>
  <files>src/components/landing/workflow-step.tsx, src/components/landing/cta-section.tsx, src/test/ai-workflow.test.tsx</files>
  <read_first>
    - src/components/landing/feature-card.tsx (server component pattern from Plan 1)
    - src/components/ui/button.tsx (Button component for CTASection)
    - src/app/globals.css (theme tokens)
    - .planning/phases/02-content-sections/02-RESEARCH.md (Pattern 2: Icon Usage, Code Examples: Workflow Step with Arrow, Reusable CTA Section)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-05 through D-09, D-17)
  </read_first>
  <behavior>
    - Test: WorkflowStep renders with number, icon, and label props
    - Test: WorkflowStep applies numbered circle with bg-accent and white text
    - Test: CTASection renders headline, subtext, and sign-up button
    - Test: CTASection button links to /register
  </behavior>
  <action>
Create `src/components/landing/workflow-step.tsx` as a server component (no "use client").

File header:
```
// ─────────────────────────────────────────────────────────────
// Workflow Step
// ─────────────────────────────────────────────────────────────
// Individual step in the AI workflow pipeline.
// Shows a numbered circle, icon, and label.
// ─────────────────────────────────────────────────────────────
```

Interface: `WorkflowStepProps` with props:
- `number`: `number` — step number (1-4)
- `icon`: `React.ComponentType<{ className?: string }>` — Lucide icon
- `label`: `string` — short step label

Component structure:
- Outer div: `flex flex-col items-center text-center`
- Numbered circle: `flex h-12 w-12 items-center justify-center rounded-full bg-accent text-white font-heading font-bold text-lg` — displays the number
- Icon: `<Icon className="h-5 w-5 text-white mt-0.5" />` inside the circle, OR below the circle as `<Icon className="h-6 w-6 text-accent mt-3" />`
  - Decision: Place icon BELOW the numbered circle for clarity. Number is primary visual, icon is secondary.
- Label: `mt-2 text-sm font-medium text-text-primary`

Export as named export: `export function WorkflowStep(...)`

---

Create `src/components/landing/cta-section.tsx` as a server component (no "use client").

File header:
```
// ─────────────────────────────────────────────────────────────
// CTA Section
// ─────────────────────────────────────────────────────────────
// Reusable sign-up call-to-action banner.
// Server component with Link wrapping Button.
// ─────────────────────────────────────────────────────────────
```

Import `Link` from `next/link` and `Button` from `@/components/ui/button`.

Interface: `CTASectionProps` with props:
- `headline`: `string` — section heading
- `subtext`: `string` — supporting text

Component structure:
- Section: `py-16 bg-white`
- Container: `max-w-6xl mx-auto px-4 text-center`
- Headline: `font-heading text-3xl font-bold text-text-primary`
- Subtext: `mt-4 text-text-secondary max-w-xl mx-auto`
- Button wrapper: `mt-8`
- Link to `/register` with Button `variant="gradient" size="lg"` and text "Sign Up for Free"

Export as named export: `export function CTASection(...)`

---

Create `src/test/ai-workflow.test.tsx` with smoke tests:
- `@vitest-environment jsdom` directive
- Mock `server-only`, `next/link`, `lucide-react` (Search, Target, FileText, BarChart3)
- Test: WorkflowStep renders number and label
- Test: CTASection renders headline and links to /register
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npx vitest run src/test/ai-workflow.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/landing/workflow-step.tsx exists and exports `WorkflowStep`
    - src/components/landing/cta-section.tsx exists and exports `CTASection`
    - WorkflowStep has no "use client" directive
    - CTASection has no "use client" directive
    - WorkflowStep numbered circle uses `bg-accent` and `text-white`
    - CTASection button uses `variant="gradient"` and links to `/register`
    - src/test/ai-workflow.test.tsx passes
    - `npm run typecheck` passes
    - `npm run lint` passes
  </acceptance_criteria>
  <done>WorkflowStep and CTASection components created and tested</done>
</task>

<task type="auto">
  <name>Task 2: Create AIWorkflow section with pipeline and integrate into page</name>
  <files>src/components/landing/ai-workflow.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/landing/workflow-step.tsx (just created)
    - src/components/landing/cta-section.tsx (just created)
    - src/components/landing/feature-showcase.tsx (previous section pattern)
    - src/app/page.tsx (current structure with FeatureShowcase)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-05 through D-09, D-14, D-16)
    - .planning/phases/02-content-sections/02-RESEARCH.md (Pitfall 3: Pipeline Arrow Responsiveness)
  </read_first>
  <action>
Create `src/components/landing/ai-workflow.tsx` as a server component (no "use client").

File header:
```
// ─────────────────────────────────────────────────────────────
// AI Workflow Preview Section
// ─────────────────────────────────────────────────────────────
// 4-step horizontal pipeline showing upcoming AI features.
// Responsive: horizontal on desktop, vertical on mobile.
// Server component — static content.
// ─────────────────────────────────────────────────────────────
```

Import WorkflowStep from `./workflow-step` and Lucide icons: Search, Target, FileText, BarChart3.

Section structure:
- Outer section: `py-24 bg-gradient-to-b from-slate-50 to-white` (subtle gradient per D-14)
- Container: `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8`
- Section heading: `text-center mb-16`
  - Headline: `font-heading text-3xl font-bold text-text-primary` — "Your AI-powered job search" (per D-08)
  - Subtitle: `mt-4 text-text-secondary max-w-2xl mx-auto` — "We're building intelligent tools to supercharge your job application process. Here's what's coming."
- Pipeline container: `flex flex-col items-center gap-6 md:flex-row md:justify-between md:gap-4` (responsive: vertical on mobile, horizontal on desktop per Pitfall 3)

Pipeline steps (per D-06, D-07):
1. `number: 1, icon: Search, label: "Resume Analysis"`
2. `number: 2, icon: Target, label: "Job Matching"`
3. `number: 3, icon: FileText, label: "Cover Letters"`
4. `number: 4, icon: BarChart3, label: "ATS Score"`

Arrow connectors between steps:
- Desktop: Right arrow `→` rendered as a text character or simple SVG, with `hidden md:block text-text-secondary text-xl`
- Mobile: Down arrow `↓` rendered as a text character, with `block md:hidden text-text-secondary text-xl`
- Arrow wrapper: `flex items-center justify-center` with appropriate responsive show/hide classes

The pipeline HTML structure (pseudo):
```
<div class="flex flex-col items-center gap-6 md:flex-row md:justify-between">
  <WorkflowStep number={1} icon={Search} label="Resume Analysis" />
  <div class="hidden md:block text-text-secondary text-xl">→</div>
  <div class="block md:hidden text-text-secondary text-xl">↓</div>
  <WorkflowStep number={2} icon={Target} label="Job Matching" />
  <div class="hidden md:block text-text-secondary text-xl">→</div>
  <div class="block md:hidden text-text-secondary text-xl">↓</div>
  <WorkflowStep number={3} icon={FileText} label="Cover Letters" />
  <div class="hidden md:block text-text-secondary text-xl">→</div>
  <div class="block md:hidden text-text-secondary text-xl">↓</div>
  <WorkflowStep number={4} icon={BarChart3} label="ATS Score" />
</div>
```

Export as named export: `export function AIWorkflow()`

---

Update `src/app/page.tsx`:
- Import AIWorkflow from `@/components/landing/ai-workflow`
- Import CTASection from `@/components/landing/cta-section`
- Add `<AIWorkflow />` after `<FeatureShowcase />`
- Add `<CTASection headline="Ready to build your resume?" subtext="Join thousands of job seekers who trust AutoApply." />` after AIWorkflow
- Page order: Navbar → Hero → FeatureShowcase → AIWorkflow → CTASection
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run typecheck && npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/landing/ai-workflow.tsx exists and exports `AIWorkflow`
    - Section renders 4 WorkflowStep components with correct numbers, icons, and labels
    - Pipeline uses `flex-col md:flex-row` for responsive layout
    - Arrow connectors use `hidden md:block` for desktop and `block md:hidden` for mobile
    - Section headline is "Your AI-powered job search" (D-08)
    - CTASection appears below AIWorkflow with gradient button linking to /register
    - page.tsx renders AIWorkflow and CTASection after FeatureShowcase
    - `npm run typecheck` passes
    - `npm run lint` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>AI workflow pipeline visible with 4 steps, arrows, and first CTA</done>
</task>

<task type="auto">
  <name>Task 3: Add AI workflow smoke tests and verify full page</name>
  <files>src/test/ai-workflow.test.tsx</files>
  <read_first>
    - src/components/landing/ai-workflow.tsx (just created)
    - src/components/landing/workflow-step.tsx (just created)
    - src/components/landing/cta-section.tsx (just created)
    - src/test/feature-showcase.test.tsx (test pattern from Plan 1)
  </read_first>
  <action>
Update `src/test/ai-workflow.test.tsx` to add tests for AIWorkflow section:
- Test: AIWorkflow renders all 4 pipeline steps
- Test: AIWorkflow renders arrow connectors
- Test: AIWorkflow section has correct heading text "Your AI-powered job search"
- Test: AIWorkflow section has gradient background classes
- Mock all Lucide icons used: Search, Target, FileText, BarChart3
- Mock WorkflowStep as a simple div that renders its props for testability
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npx vitest run src/test/ai-workflow.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ai-workflow.test.tsx passes all tests
    - Tests verify 4 steps render with correct labels
    - Tests verify section heading text
    - `npm run test` passes (no regressions)
    - `npm run typecheck` passes
    - `npm run lint` passes
  </acceptance_criteria>
  <done>All AI workflow tests pass; full page builds and renders correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client rendering | Static HTML rendered server-side, no user input crossing trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | CTA link targets | low | accept | Links use next/link with relative /register path; no external URLs |
| T-02-04 | Information Disclosure | Pipeline labels | low | accept | Labels describe upcoming features, not implementation details |
</threat_model>

<verification>
1. `npm run typecheck` — passes
2. `npm run lint` — passes (0 errors)
3. `npm run build` — passes
4. `npm run test -- --testNamePattern="workflow"` — all tests pass
5. Visual check: AI workflow section visible below feature showcase
6. Visual check: 4 steps with arrows on desktop, vertical stack on mobile
7. Visual check: CTA button below pipeline links to /register
8. Responsive check: Pipeline is horizontal at 1024px+, vertical at 375px
</verification>

<success_criteria>
- AI workflow pipeline renders 4 steps with arrow connectors
- Pipeline is responsive (horizontal desktop, vertical mobile)
- CTASection renders below pipeline with sign-up button
- All automated tests pass
- Page builds without errors
</success_criteria>

<output>
Create `.planning/phases/02-content-sections/02-02-SUMMARY.md` when done
</output>
