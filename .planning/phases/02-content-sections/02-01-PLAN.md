---
phase: 02-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/landing/feature-card.tsx
  - src/components/landing/feature-showcase.tsx
  - src/components/landing/hero.tsx
  - src/app/page.tsx
  - src/test/feature-showcase.test.tsx
autonomous: true
requirements: [LP-02]

must_haves:
  truths:
    - "Feature showcase section is visible below the hero on the landing page"
    - "6 feature cards display in a 2x3 responsive grid (1 col mobile, 2 col tablet, 3 col desktop)"
    - "Each card shows a Lucide icon, title, and 1-2 sentence description"
    - "Hero gradient fades to white at the bottom, transitioning smoothly to the feature section"
    - "Feature section has a subtle gradient background consistent with D-14"
  artifacts:
    - src/components/landing/feature-card.tsx
    - src/components/landing/feature-showcase.tsx
    - src/components/landing/hero.tsx (modified)
    - src/app/page.tsx (modified)
    - src/test/feature-showcase.test.tsx
  key_links:
    - "hero.tsx bottom gradient connects to feature-showcase.tsx top background"
    - "page.tsx renders FeatureShowcase below Hero in correct DOM order"
---

<objective>
Create the Feature Showcase section (LP-02): a 2x3 responsive grid of 6 feature cards below the hero, with a gradient fade transition from hero to content.

Purpose: First content section visitors see after the hero. Establishes the product tour feel and demonstrates AutoApply's current capabilities. The hero fade (D-15) overrides Phase 1's sharp edge decision (D-03) and must be applied here since the feature section is directly below the hero.

Output: FeatureCard component, FeatureShowcase section, hero gradient fade fix, page integration, and smoke tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/01-foundation-hero/01-03-SUMMARY.md
@src/app/page.tsx
@src/components/landing/hero.tsx
@src/components/landing/landing-layout.tsx
@src/components/ui/button.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FeatureCard server component</name>
  <files>src/components/landing/feature-card.tsx, src/test/feature-showcase.test.tsx</files>
  <read_first>
    - src/components/landing/hero.tsx (for gradient and styling patterns)
    - src/components/ui/button.tsx (for component interface patterns)
    - src/app/globals.css (for theme tokens: hero-start, text-primary, text-secondary)
    - .planning/phases/02-content-sections/02-RESEARCH.md (Pattern 1: Server Component, Pattern 2: Icon Usage)
  </read_first>
  <behavior>
    - Test: FeatureCard renders with icon, title, and description props
    - Test: FeatureCard applies correct Tailwind classes for card styling (rounded-xl, border, shadow-sm)
    - Test: FeatureCard applies hover effect classes (motion-safe:hover:shadow-md, motion-safe:hover:-translate-y-1)
    - Test: FeatureCard icon is wrapped in a container with bg-hero-start/10 background
  </behavior>
  <action>
Create `src/components/landing/feature-card.tsx` as a server component (no "use client" directive). This is a static presentational component with no interactivity.

Interface: `FeatureCardProps` with three props:
- `icon`: `React.ComponentType<{ className?: string }>` — accepts Lucide icon components
- `title`: `string` — feature name
- `description`: `string` — 1-2 sentence description

Component structure:
- Outer div: `rounded-xl border border-slate-200 bg-white p-6 shadow-sm transition-all motion-safe:hover:shadow-md motion-safe:hover:-translate-y-1`
- Icon container: `mb-4 inline-flex h-12 w-12 items-center justify-center rounded-lg bg-hero-start/10`
- Icon rendered as `<Icon className="h-6 w-6 text-hero-start" />`
- Title: `font-heading text-lg font-semibold text-text-primary`
- Description: `mt-2 text-sm text-text-secondary`

File header comment following the pattern from `landing-layout.tsx`:
```
// ─────────────────────────────────────────────────────────────
// Feature Card
// ─────────────────────────────────────────────────────────────
// Individual feature card for the feature showcase section.
// Server component — static content with Tailwind hover effects.
// ─────────────────────────────────────────────────────────────
```

Export as named export: `export function FeatureCard(...)`

Also create `src/test/feature-showcase.test.tsx` with smoke tests for FeatureCard. Follow the pattern from `src/test/resume-card.test.tsx`:
- `@vitest-environment jsdom` directive
- Mock `server-only`, `next/link`, and `lucide-react` (mock specific icons used: FileText, User, Briefcase, GraduationCap, FolderOpen, Wrench)
- Test that FeatureCard renders title and description text
- Test that FeatureCard applies card container classes
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npx vitest run src/test/feature-showcase.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/landing/feature-card.tsx exists and exports `FeatureCard`
    - Component has no "use client" directive (server component)
    - Interface `FeatureCardProps` has `icon`, `title`, `description` props
    - Icon container uses `bg-hero-start/10` and `text-hero-start` classes
    - Card uses `rounded-xl border border-slate-200 bg-white p-6 shadow-sm`
    - Hover effects use `motion-safe:` prefix (not bare hover:)
    - src/test/feature-showcase.test.tsx exists and passes
    - `npm run typecheck` passes
    - `npm run lint` passes
  </acceptance_criteria>
  <done>FeatureCard renders correctly with icon, title, description; hover effects applied; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create FeatureShowcase section and integrate into page</name>
  <files>src/components/landing/feature-showcase.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/landing/feature-card.tsx (just created)
    - src/components/landing/landing-layout.tsx (server component wrapper pattern)
    - src/app/page.tsx (current page structure to extend)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-01 through D-04, D-14, D-16)
  </read_first>
  <action>
Create `src/components/landing/feature-showcase.tsx` as a server component (no "use client").

File header following landing-layout.tsx pattern:
```
// ─────────────────────────────────────────────────────────────
// Feature Showcase Section
// ─────────────────────────────────────────────────────────────
// 2x3 responsive grid of 6 resume builder features.
// Server component — static content, no interactivity.
// ─────────────────────────────────────────────────────────────
```

Import FeatureCard from `./feature-card` and Lucide icons from `lucide-react`.

Section structure:
- Outer section: `py-24 bg-gradient-to-b from-white to-slate-50` (subtle gradient per D-14)
- Container: `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8`
- Section heading: `text-center mb-16` with `font-heading text-3xl font-bold text-text-primary` and subtitle `mt-4 text-text-secondary`
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6` (per D-01: 2x3 grid, responsive)

6 FeatureCard instances (per D-04), each with:
1. `icon: FileText` — "Resume Creation" — "Build professional resumes with AI-powered suggestions and customizable templates."
2. `icon: User` — "Profile Management" — "Centralize your career history in one reusable profile that powers all your resumes."
3. `icon: Briefcase` — "Experience Tracking" — "Organize your work experience with detailed descriptions, dates, and achievements."
4. `icon: GraduationCap` — "Education" — "Showcase your academic background with degrees, institutions, and relevant coursework."
5. `icon: FolderOpen` — "Project Listings" — "Highlight your best projects with descriptions, links, and technology stacks."
6. `icon: Wrench` — "Skill Management" — "Curate and organize your technical and soft skills for targeted job applications."

Section heading: "Everything you need to build the perfect resume"
Section subtitle: "AutoApply gives you the tools to create, manage, and optimize your professional resume."

Export as named export: `export function FeatureShowcase()`

Update `src/app/page.tsx`:
- Import FeatureShowcase from `@/components/landing/feature-showcase`
- Add `<FeatureShowcase />` after `<Hero />` inside LandingLayout
- Page order: Navbar → Hero → FeatureShowcase
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run typecheck && npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/landing/feature-showcase.tsx exists and exports `FeatureShowcase`
    - Section renders 6 FeatureCard components with correct props
    - Grid uses `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` responsive classes
    - Section background uses `bg-gradient-to-b from-white to-slate-50`
    - page.tsx imports and renders FeatureShowcase after Hero
    - `npm run typecheck` passes
    - `npm run lint` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>Feature showcase section visible on landing page with 6 cards in responsive grid</done>
</task>

<task type="auto">
  <name>Task 3: Fix hero gradient fade and add smoke tests</name>
  <files>src/components/landing/hero.tsx, src/test/feature-showcase.test.tsx</files>
  <read_first>
    - src/components/landing/hero.tsx (current hero with sharp edge per Phase 1 D-03)
    - src/components/landing/feature-showcase.tsx (just created, starts with bg-white)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-15: hero fades to first section)
    - .planning/phases/02-content-sections/02-RESEARCH.md (Pitfall 5: gradient fade)
  </read_first>
  <action>
Modify `src/components/landing/hero.tsx` to add gradient fade at the bottom (per D-15, overriding Phase 1 D-03).

The hero section currently has class `bg-gradient-to-br from-hero-start to-hero-end`. Add a gradient fade overlay at the bottom of the hero section. The approach: add a pseudo-element or a child div at the bottom that fades from the hero gradient to white.

Implementation approach — add a gradient overlay div inside the hero section, positioned at the bottom:
- Add a new div inside the hero section, after the content div but before the style tag
- Position: `absolute bottom-0 left-0 right-0 h-32`
- Background: `bg-gradient-to-t from-white to-transparent`
- This creates a smooth transition from the hero gradient to the white/gradient background of the feature section

Update `src/test/feature-showcase.test.tsx` to add tests for FeatureShowcase:
- Test: FeatureShowcase renders all 6 feature cards
- Test: FeatureShowcase section has correct grid layout classes
- Test: FeatureShowcase section has gradient background
- Mock lucide-react icons used in FeatureShowcase (FileText, User, Briefcase, GraduationCap, FolderOpen, Wrench)
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npx vitest run src/test/feature-showcase.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - hero.tsx has a gradient overlay div at the bottom: `absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-white to-transparent`
    - Feature section starts with `bg-white` or `bg-gradient-to-b from-white` to receive the fade
    - feature-showcase.test.tsx passes all tests
    - `npm run typecheck` passes
    - `npm run lint` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>Hero fades smoothly to feature section; all feature showcase tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client rendering | Static HTML rendered server-side, no user input crossing trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Feature cards (hardcoded content) | low | accept | Content is hardcoded in source; no user input to tamper with |
| T-02-02 | Elevation | CTA links | low | accept | Links use next/link with relative paths to /register; no external URLs |
</threat_model>

<verification>
1. `npm run typecheck` — passes
2. `npm run lint` — passes (0 errors)
3. `npm run build` — passes
4. `npm run test -- --testNamePattern="feature"` — all tests pass
5. Visual check: Feature section visible below hero at localhost:3000
6. Visual check: Hero gradient fades to white at bottom
7. Responsive check: Feature grid is 1 column at 375px, 2 columns at 768px, 3 columns at 1024px+
</verification>

<success_criteria>
- Feature showcase section renders 6 cards in a responsive 2x3 grid
- Hero gradient fades smoothly to the feature section
- All automated tests pass
- Page builds without errors
</success_criteria>

<output>
Create `.planning/phases/02-content-sections/02-01-SUMMARY.md` when done
</output>
