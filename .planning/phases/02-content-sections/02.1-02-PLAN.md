---
phase: 02-content-sections
plan: "02.1-02"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/components/landing/ai-workflow.tsx
  - src/components/landing/workflow-step.tsx
  - src/components/landing/testimonials.tsx
  - src/components/landing/testimonial-card.tsx
  - src/test/ai-workflow.test.tsx
  - src/test/testimonials.test.tsx
autonomous: true
requirements: [LP-02, LP-03, LP-04]
gap_closure: true

must_haves:
  truths:
    - "AI Workflow section uses a card grid layout (grid grid-cols-1 md:grid-cols-2) instead of horizontal flex pipeline"
    - "Each WorkflowStep is a styled card with rounded-xl, shadow, and border"
    - "SVG arrow connectors appear between cards in the grid layout"
    - "WorkflowStep no longer renders numbered circles; the icon is the primary visual"
    - "Testimonials section displays 6 testimonial cards in a horizontal scroll carousel"
    - "TestimonialCard uses a photo placeholder (rounded-full bg-slate-200 with User icon) instead of initials avatar"
    - "TestimonialCard quote is bold (font-semibold text-text-primary) without quotation marks"
    - "TestimonialCard attribution is lighter (font-medium text-text-secondary)"
    - "All updated tests pass for grid layout, card styling, carousel, 6 testimonials, and new avatar"
  artifacts:
    - src/components/landing/ai-workflow.tsx (card grid layout)
    - src/components/landing/workflow-step.tsx (card styling, no numbered circle)
    - src/components/landing/testimonials.tsx (6 testimonials, carousel layout)
    - src/components/landing/testimonial-card.tsx (photo placeholder, bold quote)
    - src/test/ai-workflow.test.tsx (grid layout, card styling assertions)
    - src/test/testimonials.test.tsx (6 names, carousel, photo placeholder assertions)
  key_links:
    - "WorkflowStep card styling uses bg-ai-start from globals.css (set in Plan 1)"
    - "Testimonials section uses bg-testimonials-start from globals.css (set in Plan 1)"
    - "Arrow connectors in AI workflow grid are SVG elements between cards"
    - "Carousel scroll behavior requires snap-x snap-mandatory on flex container"
---

<objective>
Close Gap 2 (AI Workflow Layout) and Gap 4 (Testimonials Redesign) from the Phase 2 UAT diagnosis.

Purpose: Redesign the AI Workflow section from a horizontal pipeline to a card grid with arrow connectors, and redesign the Testimonials section from a 3-card grid to a 6-card horizontal scroll carousel with photo placeholders and bold quote styling.

Output: Updated AI Workflow with card grid layout, updated Testimonials with 6 cards in carousel, updated tests for both sections, all tests passing.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-UAT.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@.planning/phases/02-content-sections/02-02-SUMMARY.md
@.planning/phases/02-content-sections/02-03-SUMMARY.md
@.planning/phases/02-content-sections/02.1-01-SUMMARY.md
@src/components/landing/ai-workflow.tsx
@src/components/landing/workflow-step.tsx
@src/components/landing/testimonials.tsx
@src/components/landing/testimonial-card.tsx
@src/test/ai-workflow.test.tsx
@src/test/testimonials.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AI Workflow card grid with arrow connectors</name>
  <files>
    src/components/landing/ai-workflow.tsx,
    src/components/landing/workflow-step.tsx,
    src/test/ai-workflow.test.tsx
  </files>
  <action>
**1. Redesign WorkflowStep (src/components/landing/workflow-step.tsx):**

Remove the `number` prop from WorkflowStepProps. The step number is no longer displayed. The interface becomes:
```
interface WorkflowStepProps {
  icon: React.ComponentType<{ className?: string }>;
  label: string;
  description?: string;
}
```

Add a `description` prop for a short explanation of each step. This makes cards more informative.

Replace the current bare flex-col layout with a styled card. The new WorkflowStep renders:
- A card container div with: `rounded-xl border border-slate-200 bg-white p-6 shadow-sm`
- Inside: the icon displayed prominently (larger, `h-10 w-10 text-ai-start`) at the top
- Below icon: the label as a heading (`font-heading text-lg font-semibold text-text-primary`)
- Below label: the description text (`text-sm text-text-secondary mt-2`)
- Remove the numbered circle div entirely

The component becomes approximately:
```
<div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
  <Icon className="h-10 w-10 text-ai-start" />
  <h3 className="font-heading text-lg font-semibold text-text-primary mt-4">{label}</h3>
  {description && <p className="text-sm text-text-secondary mt-2">{description}</p>}
</div>
```

**2. Update AIWorkflow steps data (src/components/landing/ai-workflow.tsx):**

Update the steps array to include descriptions and remove the `number` property:
```
const steps = [
  { icon: Search, label: "Resume Analysis", description: "AI scans your resume for strengths, gaps, and optimization opportunities." },
  { icon: Target, label: "Job Matching", description: "Intelligent matching connects your skills to the right opportunities." },
  { icon: FileText, label: "Cover Letters", description: "Generate tailored cover letters for each application in seconds." },
  { icon: BarChart3, label: "ATS Score", description: "See how your resume scores against applicant tracking systems." },
];
```

**3. Replace the flex pipeline layout with a card grid (src/components/landing/ai-workflow.tsx):**

Replace the current container div:
```
<div className="flex flex-col items-center gap-6 md:flex-row md:justify-between md:gap-4">
```

With a grid layout that includes arrow connectors between cards:
```
<div className="grid grid-cols-1 md:grid-cols-2 gap-6 relative">
  {steps.map((step, index) => (
    <div key={step.label} className="contents">
      <WorkflowStep
        icon={step.icon}
        label={step.label}
        description={step.description}
      />
      {index < steps.length - 1 && (
        <div className="hidden md:flex items-center justify-center col-span-full" aria-hidden="true">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" className="text-text-secondary">
            <path d="M12 5v14M19 12l-7 7-7-7" />
          </svg>
        </div>
      )}
    </div>
  ))}
</div>
```

The arrow is an SVG chevron-down icon placed in a `col-span-full` div that appears between rows on desktop. On mobile, it shows naturally between vertically stacked cards. The `contents` wrapper keeps each step and its following arrow as siblings in the grid flow.

Remove the inline HTML entities (`&rarr;` and `&darr;`) and the associated flex containers for arrows.

**4. Update ai-workflow.test.tsx (src/test/ai-workflow.test.tsx):**

Update the lucide-react mock to include the new icon names if needed (Search, Target, FileText, BarChart3 are already mocked).

Update WorkflowStep tests:
- Remove the test for "renders the step number and label" that checks for `getByText("1")` -- numbered circles are gone
- Remove the test for "applies numbered circle with bg-accent and white text" -- no more rounded-full circle
- Replace with: "renders the label and description" test that checks `getByText("Resume Analysis")` and description text
- Replace the "applies correct layout classes" test to check for card classes: `rounded-xl`, `border`, `bg-white`, `p-6`, `shadow-sm`
- Keep the "renders the icon" test

Update AIWorkflow tests:
- "renders all 4 pipeline steps" -- change to "renders all 4 workflow cards" and verify the 4 labels exist
- "renders arrow connectors" -- update to check for SVG elements with chevron path (e.g., querySelector for svg with path, or check for the col-span-full arrow divs)
- "pipeline uses responsive flex layout" -- replace with "uses grid layout" that checks for `grid` class and `md:grid-cols-2`
- "has gradient background classes" -- update to check for `bg-ai-start` instead of `bg-gradient-to-b from-slate-50 to-white`
- Add a new test: "each step card has rounded-xl, shadow, and border classes" by rendering a WorkflowStep and checking the card container
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run test -- --filter=ai-workflow 2>&1 | tail -20</automated>
  </verify>
  <done>
    - WorkflowStep renders a styled card (rounded-xl, border, shadow, bg-white)
    - WorkflowStep has no numbered circle, icon is primary visual
    - WorkflowStep accepts optional description prop
    - AIWorkflow uses grid layout (grid grid-cols-1 md:grid-cols-2)
    - Arrow SVG connectors appear between card rows
    - Steps array includes descriptions
    - ai-workflow.test.tsx tests pass for grid layout, card styling, arrow connectors
    - ai-workflow.test.tsx asserts bg-ai-start on section
    - npm run test passes for ai-workflow
    - npm run typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Testimonials carousel with 6 cards and photo placeholders</name>
  <files>
    src/components/landing/testimonials.tsx,
    src/components/landing/testimonial-card.tsx,
    src/test/testimonials.test.tsx
  </files>
  <action>
**1. Add 3 new testimonials to the data array (src/components/landing/testimonials.tsx):**

Expand the testimonials array from 3 to 6 entries. Add these 3 new testimonials after the existing 3:

```
{
  name: "Alex Rivera",
  title: "Frontend Developer",
  company: "Vercel",
  quote: "The template system is clean and professional. I landed three interviews in my first week using AutoApply.",
},
{
  name: "Jamie Nguyen",
  title: "DevOps Engineer",
  company: "Cloudflare",
  quote: "Finally a resume tool that understands technical roles. The skills section alone saved me hours of formatting.",
},
{
  name: "Taylor Kim",
  title: "UX Designer",
  company: "Figma",
  quote: "AutoApply's profile system meant I could create role-specific resumes without starting from scratch each time.",
},
```

**2. Replace CSS grid with horizontal scroll carousel (src/components/landing/testimonials.tsx):**

Replace the grid container:
```
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
```

With a horizontal scroll carousel:
```
<div className="flex gap-6 overflow-x-auto snap-x snap-mandatory pb-4 -mx-4 px-4 sm:mx-0 sm:px-0">
  {testimonials.map((testimonial) => (
    <div key={testimonial.name} className="snap-center shrink-0 w-[85vw] sm:w-[400px]">
      <TestimonialCard {...testimonial} />
    </div>
  ))}
</div>
```

Each card wrapper has `shrink-0` to prevent flex shrinking, `w-[85vw] sm:w-[400px]` for responsive card width, and `snap-center` for snap scrolling. The container has `overflow-x-auto snap-x snap-mandatory` for the carousel behavior. The `-mx-4 px-4` negative margin trick allows cards to bleed to screen edges on mobile.

**3. Redesign TestimonialCard (src/components/landing/testimonial-card.tsx):**

Remove the initials computation (lines 21-26). Replace the avatar section:

Old:
```
const initials = name.split(" ").map((w) => w[0]).join("").slice(0, 2).toUpperCase();
...
<div className="mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-hero-start text-white font-heading font-bold text-sm">
  {initials}
</div>
```

New (photo placeholder with User icon):
```
import { User } from "lucide-react";
...
<div className="mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-slate-200">
  <User className="h-6 w-6 text-slate-500" />
</div>
```

Note: This component still uses Lucide for the User icon in the avatar placeholder. This is acceptable -- the gap closure requirement was specifically about the feature showcase icons.

Replace the quote styling:

Old: `<p className="text-text-secondary italic">&ldquo;{quote}&rdquo;</p>`
New: `<p className="text-text-primary font-semibold">{quote}</p>`

Remove the italic class, remove the quotation marks (ldquo/rdquo), change from text-text-secondary to text-text-primary, and add font-semibold.

Update the attribution styling:

Old name line: `<p className="font-heading text-sm font-semibold text-text-primary">{name}</p>`
New name line: `<p className="font-medium text-text-secondary">{name}</p>`

Remove font-heading, change from font-semibold to font-medium, change from text-text-primary to text-text-secondary.

The company/title line stays the same: `<p className="text-xs text-text-secondary">{title} at {company}</p>`

**4. Update testimonials.test.tsx (src/test/testimonials.test.tsx):**

Update TestimonialCard tests:
- Remove "displays initials in avatar circle" test (getByText("SC") no longer exists)
- Replace with: "displays photo placeholder avatar" test that checks for the User icon or the `bg-slate-200` class on the avatar container
- Remove "quote contains quotation marks" test (no more ldquo/rdquo)
- Replace with: "quote is bold and has no quotation marks" test that checks the quote element has `font-semibold` class and `text-text-primary` class
- Update "applies circle avatar with bg-hero-start and white text" to check for `bg-slate-200` instead of `bg-hero-start` and `text-white`
- Add test for attribution: "attribution has lighter styling" that checks name element has `font-medium` and `text-text-secondary`

Update Testimonials tests:
- "renders all 3 testimonial cards" -- change to "renders all 6 testimonial cards" and add assertions for all 6 names: Sarah Chen, Marcus Johnson, Priya Patel, Alex Rivera, Jamie Nguyen, Taylor Kim
- "grid uses responsive layout classes" -- replace with "uses carousel layout" that checks for `overflow-x-auto`, `snap-x`, `snap-mandatory` on the container, and checks that card wrappers have `snap-center` and `shrink-0`
- "has gradient background classes" -- update to check for `bg-testimonials-start` instead of `bg-gradient-to-b from-white to-slate-50`
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run test -- --filter=testimonials 2>&1 | tail -20</automated>
  </verify>
  <done>
    - testimonials.tsx has 6 testimonial entries
    - testimonials.tsx uses flex carousel layout (overflow-x-auto, snap-x, snap-mandatory)
    - testimonial-card.tsx uses photo placeholder (rounded-full bg-slate-200 with User icon)
    - testimonial-card.tsx quote is bold (font-semibold text-text-primary) with no quotation marks
    - testimonial-card.tsx attribution is lighter (font-medium text-text-secondary)
    - testimonials.test.tsx asserts 6 names
    - testimonials.test.tsx asserts carousel layout classes
    - testimonials.test.tsx asserts photo placeholder avatar
    - testimonials.test.tsx asserts bold quote styling
    - testimonials.test.tsx asserts bg-testimonials-start on section
    - npm run test passes for testimonials
    - npm run typecheck passes
    - npm run lint passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Horizontal scroll on mobile | Ensure scroll behavior does not cause layout shift or performance issues |
| Lucide User icon in testimonial-card | Small dependency, already in project |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan -- only component changes and test updates |
| T-02.1-02-01 | Denial of Service | Carousel scroll performance | low | accept | 6 cards with snap-x is lightweight, no virtualization needed |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm run lint` passes
- `npm run test -- --filter=ai-workflow` passes
- `npm run test -- --filter=testimonials` passes
- `npm run test` (full suite) passes
- AI Workflow section renders 4 cards in a 2x2 grid with SVG arrow connectors
- Testimonials section renders 6 cards in a horizontal scroll carousel
- No numbered circles in WorkflowStep
- Photo placeholder avatars in TestimonialCard
- Bold quotes without quotation marks
</verification>

<success_criteria>
- AI Workflow uses card grid layout with arrow connectors instead of horizontal pipeline
- WorkflowStep is a styled card with icon as primary visual, no numbered circles
- Testimonials has 6 cards in horizontal scroll carousel
- TestimonialCard uses photo placeholder avatar, bold quote, lighter attribution
- All tests pass for both sections
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/02-content-sections/02.1-02-SUMMARY.md` when done
</output>
