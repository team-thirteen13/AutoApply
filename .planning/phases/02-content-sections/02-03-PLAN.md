---
phase: 02-content-sections
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/landing/testimonial-card.tsx
  - src/components/landing/testimonials.tsx
  - src/app/page.tsx
  - src/test/testimonials.test.tsx
autonomous: true
requirements: [LP-04]

must_haves:
  truths:
    - "Testimonials section is visible below the AI workflow CTA"
    - "3 testimonial cards display in a responsive grid (1 col mobile, 2 col tablet, 3 col desktop)"
    - "Each card shows a circle avatar with initials, quote with quotation marks, name, title, and company"
    - "A second sign-up CTA appears below the testimonials (D-09, D-17)"
    - "All content sections maintain readability on mobile viewports"
  artifacts:
    - src/components/landing/testimonial-card.tsx
    - src/components/landing/testimonials.tsx
    - src/app/page.tsx (modified)
    - src/test/testimonials.test.tsx
  key_links:
    - "CTASection (from Plan 2) is reused for the second CTA"
    - "page.tsx renders Testimonials and second CTASection in correct order"
    - "All sections flow: Hero → Features → AI Workflow → CTA → Testimonials → CTA"
---

<objective>
Create the Testimonials section (LP-04): 3 testimonial cards with circle avatars, quotes, and attribution, plus the second sign-up CTA below.

Purpose: Provides social proof to build visitor confidence. The second CTA (D-17) maximizes conversion opportunities after visitors have seen all content sections.

Output: TestimonialCard component, Testimonials section, page integration with second CTA, and smoke tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@.planning/phases/02-content-sections/02-02-SUMMARY.md
@src/app/page.tsx
@src/components/landing/cta-section.tsx
@src/components/ui/button.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TestimonialCard component</name>
  <files>src/components/landing/testimonial-card.tsx, src/test/testimonials.test.tsx</files>
  <read_first>
    - src/components/landing/feature-card.tsx (server component pattern from Plan 1)
    - src/app/globals.css (theme tokens)
    - .planning/phases/02-content-sections/02-RESEARCH.md (Pattern 1: Server Component)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-10 through D-13)
  </read_first>
  <behavior>
    - Test: TestimonialCard renders with name, title, company, and quote props
    - Test: TestimonialCard displays circle avatar with initials derived from name
    - Test: TestimonialCard quote text is styled with italic and quotation marks
    - Test: TestimonialCard displays name, title, and company below the quote
  </behavior>
  <action>
Create `src/components/landing/testimonial-card.tsx` as a server component (no "use client").

File header:
```
// ─────────────────────────────────────────────────────────────
// Testimonial Card
// ─────────────────────────────────────────────────────────────
// Individual testimonial card with avatar, quote, and attribution.
// Server component — static content.
// ─────────────────────────────────────────────────────────────
```

Interface: `TestimonialCardProps` with props:
- `name`: `string` — person's full name
- `title`: `string` — job title
- `company`: `string` — company name
- `quote`: `string` — testimonial text

Component structure:
- Outer div: `rounded-xl border border-slate-200 bg-white p-6 shadow-sm`
- Avatar: Circle with initials derived from name (first letter of first and last name)
  - Container: `mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-hero-start text-white font-heading font-bold text-sm`
  - Initials logic: Split name by space, take first char of first and last word, join. If single word, use first two chars. For display: `{name.split(" ").map(w => w[0]).join("").slice(0, 2).toUpperCase()}`
- Quote: Wrapped in quotation marks with italic styling
  - `<p className="text-text-secondary italic">"{quote}"</p>` — the quotation marks are literal characters in the JSX, not CSS
- Attribution block: `mt-4`
  - Name: `font-heading text-sm font-semibold text-text-primary`
  - Title + Company: `text-xs text-text-secondary` — "{title} at {company}"

Export as named export: `export function TestimonialCard(...)`

---

Create `src/test/testimonials.test.tsx` with smoke tests:
- `@vitest-environment jsdom` directive
- Mock `server-only`
- Test: TestimonialCard renders name, title, company, and quote
- Test: TestimonialCard displays initials in avatar circle
- Test: TestimonialCard quote contains quotation marks
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npx vitest run src/test/testimonials.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/landing/testimonial-card.tsx exists and exports `TestimonialCard`
    - Component has no "use client" directive
    - Interface `TestimonialCardProps` has `name`, `title`, `company`, `quote` props
    - Avatar circle uses `bg-hero-start text-white rounded-full`
    - Quote text is italic with literal quotation marks
    - Initials are derived from name prop (first letters of first and last name)
    - src/test/testimonials.test.tsx passes
    - `npm run typecheck` passes
    - `npm run lint` passes
  </acceptance_criteria>
  <done>TestimonialCard renders correctly with avatar, quote, and attribution; tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create Testimonials section and integrate into page</name>
  <files>src/components/landing/testimonials.tsx, src/app/page.tsx</files>
  <read_first>
    - src/components/landing/testimonial-card.tsx (just created)
    - src/components/landing/cta-section.tsx (reusable CTA from Plan 2)
    - src/components/landing/ai-workflow.tsx (previous section pattern)
    - src/app/page.tsx (current structure with AIWorkflow and CTASection)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-10 through D-13, D-14, D-16, D-17)
  </read_first>
  <action>
Create `src/components/landing/testimonials.tsx` as a server component (no "use client").

File header:
```
// ─────────────────────────────────────────────────────────────
// Testimonials Section
// ─────────────────────────────────────────────────────────────
// 3-column grid of testimonial cards with social proof.
// Server component — static content with placeholder data.
// ─────────────────────────────────────────────────────────────
```

Import TestimonialCard from `./testimonial-card`.

Section structure:
- Outer section: `py-24 bg-gradient-to-b from-white to-slate-50` (subtle gradient per D-14)
- Container: `max-w-6xl mx-auto px-4 sm:px-6 lg:px-8`
- Section heading: `text-center mb-16`
  - Headline: `font-heading text-3xl font-bold text-text-primary` — "Loved by job seekers"
  - Subtitle: `mt-4 text-text-secondary` — "See what our users have to say about AutoApply."
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` (per D-10: 3-column grid, responsive)

3 TestimonialCard instances (per D-13, placeholder data per LP-04):
1. `name: "Sarah Chen"`, `title: "Software Engineer"`, `company: "Google"`, `quote: "AutoApply helped me craft a resume that actually got noticed. The AI suggestions were spot-on for my industry."`
2. `name: "Marcus Johnson"`, `title: "Product Manager"`, `company: "Stripe"`, `quote: "I was spending hours tweaking my resume for each application. AutoApply made it effortless to create targeted versions."`
3. `name: "Priya Patel"`, `title: "Data Scientist"`, `company: "Meta"`, `quote: "The profile system is brilliant — I update once and every resume stays in sync. Saved me so much time during my job search."`

Export as named export: `export function Testimonials()`

---

Update `src/app/page.tsx`:
- Import Testimonials from `@/components/landing/testimonials`
- Add `<Testimonials />` after the first `<CTASection />`
- Add a second `<CTASection />` after Testimonials with different copy:
  - `headline: "Start building your future today"`
  - `subtext: "Create your free account and build a resume that gets results."`
- Page order: Navbar → Hero → FeatureShowcase → AIWorkflow → CTASection (1st) → Testimonials → CTASection (2nd)
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npm run typecheck && npm run lint && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/landing/testimonials.tsx exists and exports `Testimonials`
    - Section renders 3 TestimonialCard components with correct props
    - Grid uses `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` responsive classes
    - Section heading is "Loved by job seekers"
    - Second CTASection appears after Testimonials with unique headline
    - page.tsx renders Testimonials and second CTASection in correct order
    - Complete page flow: Hero → Features → AIWorkflow → CTA → Testimonials → CTA
    - `npm run typecheck` passes
    - `npm run lint` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>Testimonials section visible with 3 cards; second CTA below; full page flow complete</done>
</task>

<task type="auto">
  <name>Task 3: Add testimonial smoke tests and verify full page end-to-end</name>
  <files>src/test/testimonials.test.tsx</files>
  <read_first>
    - src/components/landing/testimonials.tsx (just created)
    - src/components/landing/testimonial-card.tsx (just created)
    - src/test/feature-showcase.test.tsx (test pattern reference)
    - src/test/ai-workflow.test.tsx (test pattern reference)
  </read_first>
  <action>
Update `src/test/testimonials.test.tsx` to add tests for Testimonials section:
- Test: Testimonials renders all 3 testimonial cards
- Test: Testimonials section has correct heading text "Loved by job seekers"
- Test: Testimonials section has gradient background classes
- Mock TestimonialCard as a simple component that renders name and quote for testability
  </action>
  <verify>
    <automated>cd /Users/yarzarmyomin/Workspace/Personal/Vibe_Workspace/vibe-code-tours/team-project/AutoApply && npx vitest run src/test/testimonials.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - testimonials.test.tsx passes all tests
    - Tests verify 3 cards render with correct names
    - Tests verify section heading text
    - `npm run test` passes (no regressions across all test files)
    - `npm run typecheck` passes
    - `npm run lint` passes
    - `npm run build` passes
  </acceptance_criteria>
  <done>All testimonial tests pass; complete landing page renders all sections correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client rendering | Static HTML rendered server-side, no user input crossing trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | Testimonial content | low | accept | Content is hardcoded placeholder data; no user-supplied testimonials in v1 |
| T-02-06 | Elevation | Second CTA link | low | accept | Links use next/link with relative /register path; no external URLs |
</threat_model>

<verification>
1. `npm run typecheck` — passes
2. `npm run lint` — passes (0 errors)
3. `npm run build` — passes
4. `npm run test` — all tests pass (feature-showcase, ai-workflow, testimonials)
5. Visual check: Testimonials section visible with 3 cards
6. Visual check: Second CTA below testimonials
7. Visual check: Full page flow — Hero → Features → AIWorkflow → CTA → Testimonials → CTA
8. Responsive check: Testimonials grid is 1 column at 375px, 2 columns at 768px, 3 columns at 1024px+
9. Mobile check: All sections readable, no horizontal scrolling
</verification>

<success_criteria>
- Testimonials section renders 3 cards with avatars, quotes, and attribution
- Second CTA appears below testimonials
- Full page flow is complete: Hero → Features → AIWorkflow → CTA → Testimonials → CTA
- All automated tests pass
- Page builds without errors
- All sections responsive on mobile, tablet, and desktop
</success_criteria>

<output>
Create `.planning/phases/02-content-sections/02-03-SUMMARY.md` when done
</output>
